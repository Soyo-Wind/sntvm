name: Rust Cross-Platform Release

permissions:
  contents: write  # Release 作成＆アセットアップロードに必要

on:
  push:
    tags:
      - 'v*.*.*'   # v1.0.0 形式のタグを push したときだけ実行

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      # 1. コードをチェックアウト
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Rust セットアップ
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      # 3. Cargo 依存キャッシュ
      - name: Cache Cargo registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      # 4. リリースビルド
      - name: Build Release
        run: cargo build --release

      # 5. バイナリをパッケージ化
      - name: Package Binary
        run: |
          mkdir -p release
          BIN_NAME=sntvm
          TAG_NAME=${GITHUB_REF#refs/tags/}  # push タグ名を取得

          if [[ "${RUNNER_OS}" == "Windows" ]]; then
            zip release/${BIN_NAME}-${TAG_NAME}-windows.zip target/release/${BIN_NAME}.exe
          elif [[ "${RUNNER_OS}" == "macOS" ]]; then
            tar czf release/${BIN_NAME}-${TAG_NAME}-macos.tar.gz -C target/release ${BIN_NAME}
          else
            tar czf release/${BIN_NAME}-${TAG_NAME}-linux.tar.gz -C target/release ${BIN_NAME}
          fi

      # 6. GitHub Release 作成
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 7. Release アセットをアップロード
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ runner.os == 'Windows' && 'release/sntvm-${GITHUB_REF#refs/tags/}-windows.zip' || runner.os == 'macOS' && 'release/sntvm-${GITHUB_REF#refs/tags/}-macos.tar.gz' || 'release/sntvm-${GITHUB_REF#refs/tags/}-linux.tar.gz' }}
          asset_name: ${{ runner.os == 'Windows' && 'sntvm-${GITHUB_REF#refs/tags/}-windows.zip' || runner.os == 'macOS' && 'sntvm-${GITHUB_REF#refs/tags/}-macos.tar.gz' || 'sntvm-${GITHUB_REF#refs/tags/}-linux.tar.gz' }}
          asset_content_type: application/octet-stream
